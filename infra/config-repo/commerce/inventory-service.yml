server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_db
    username: dbuser
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false